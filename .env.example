# Copy to .env and fill in real values. Never commit .env itself.

VAPI_API_KEY=
VAPI_PHONE_NUMBER_ID=
EVERLY_ASSISTANT_ID=

MONGODB_URI=

# Add Twilio vars only if bypassing Vapi's API directly (not needed for normal calling).
# Add MDR_API_BASE_URL / MDR_API_KEY once MDR provides access.
